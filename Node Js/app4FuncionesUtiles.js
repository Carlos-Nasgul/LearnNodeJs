//PARA EJECUTAR EL CÓDIGO DESPUES DE (X) SEG O MLSEG.
/**
 * setTimeout() 
 * setTimeout(funcion, retraso(mlsegundos), argumento);
 */

function mostrarPrueba(tema) {
    console.log(`Estoy probando el delayed ${tema}`);
}

setTimeout(mostrarPrueba, 4000, 'Node');

//setInmediat()
/**
 * PARA EJECUTAR CÓDIGO ASÍNCRONO
 * EN LA PROXIMA ITERACIÓN DEL CICLO DE EVENTOS
 * (LO MÁS PRONTO POSIBLE).
 */




//setInterval()

/**
 * 
 */